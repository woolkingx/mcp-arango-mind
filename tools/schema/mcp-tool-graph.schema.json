{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "mcp-tool-graph.schema.json",
  "title": "MCP Tool Graph",
  "description": "ArangoDB graph category scoped to the Graphs tag.",
  "type": "object",
  "additionalProperties": false,
  "required": ["action", "payload"],
  "properties": {
    "action": {
      "type": "string",
      "enum": ["list", "search", "describe", "call"]
    },
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "target": { "type": "string", "description": "OpenAPI operationId within the Graphs tag." },
        "params": { "type": "object" },
        "keywords": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
        "limit": { "type": "integer", "minimum": 1, "maximum": 500, "default": 100 },
        "format": { "type": "string", "enum": ["json", "md"], "default": "md" }
      }
    }
  },
  "allOf": [
    { "if": { "properties": { "action": { "const": "search" } } }, "then": { "properties": { "payload": { "required": ["keywords"] } } } },
    { "if": { "properties": { "action": { "const": "describe" } } }, "then": { "properties": { "payload": { "required": ["target"] } } } },
    { "if": { "properties": { "action": { "const": "call" } } }, "then": { "properties": { "payload": { "required": ["target"] } } } }
  ],
  "x-tool": {
    "name": "mcp.tool.graph",
    "title": "MCP Tool Graph",
    "description": "ArangoDB graph category scoped to the Graphs tag.",
    "target": "mcp.tool.graph",
    "actions": {
      "list":     { "handler": "categoryGraph.list",     "effect": "read",  "idempotent": true,  "openWorld": false, "evidence": "Graphs-tag operation listing" },
      "search":   { "handler": "categoryGraph.search",   "effect": "read",  "idempotent": true,  "openWorld": false, "evidence": "Graphs-tag operation matches" },
      "describe": { "handler": "categoryGraph.describe", "effect": "read",  "idempotent": true,  "openWorld": false, "evidence": "Graphs-tag operation contract" },
      "call":     { "handler": "categoryGraph.call",     "effect": "write", "idempotent": false, "openWorld": true,  "evidence": "ArangoDB Graph operation response" }
    }
  }
}
