# mcp-arango-mind Architecture

Architecture truth lives in the handbook:

```text
docs/handbook/index.html
```

Current topology:

```text
interface -> tools -> arango
```

Current schema roots:

```text
mcp/schema/mcp.schema.json
tools/schema/tools.schema.json
arango/schema/arango.openapi.schema.json
```

This file is only a compatibility pointer for older links. Do not add
architecture decisions here.
